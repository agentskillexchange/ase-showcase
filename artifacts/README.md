# Artifacts

Sanitized, representative excerpts of the real system — included to make the engineering concrete.
None of these contain credentials, hostnames, IPs, or secrets.

| File | What it is | Referenced from |
|------|------------|-----------------|
| [`star-guard.diff`](star-guard.diff) | The read-side guard that nulls monorepo-inherited star counts (the workaround later superseded by the source-side fix). Real, unmodified diff — it only references the public domain and the catalog's `SKILL.md` layout. | [docs/06](../docs/06-case-study-star-bug.md) |
| [`cron-inventory.sample.md`](cron-inventory.sample.md) | The ten-cron schedule + model + write-target map, with infrastructure abstracted. | [docs/02](../docs/02-autonomous-pipeline.md) |
| [`openclaw.manifest.schema.json`](openclaw.manifest.schema.json) | JSON Schema for the `openclaw-skills/1.0` agent-discovery manifests (`openclaw.json` / `codex.json` / `skills.json`). | [docs/01](../docs/01-system-architecture.md) |
| [`enrichment-batch.example.json`](enrichment-batch.example.json) | A synthetic enrichment record set illustrating the signals the sweep manages — including the monorepo-subdir case where `github_stars` is correctly dropped. | [docs/06](../docs/06-case-study-star-bug.md) |

> The values in the `.json` examples are illustrative/synthetic. The live manifests are generated by
> the WordPress `ase-marketplace-layer` plugin and served at the site root.
>
> `star-guard.diff` is a real, unmodified diff; its log line refers to fixing the issue in "the source
> enrichment (Prompt 1)" — an internal label for the upstream enrichment task. In this repo's
> narrative that upstream fix is [Fix B, the source-side fix](../docs/06-case-study-star-bug.md).
